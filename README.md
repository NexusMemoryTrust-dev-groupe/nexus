<div align="center">

# Nexus Memory Trust

### Операционная система памяти для искусственного интеллекта

**Новая парадигма работы с памятью. Ваш ИИ больше не забывает.**

[![Version](https://img.shields.io/badge/version-1.2.0-2ea043?style=for-the-badge)](https://github.com/NexusMemoryTrust-dev-groupe/nexus/releases)
[![Platform](https://img.shields.io/badge/platform-Windows%2010%20%7C%2011-0078d4?style=for-the-badge&logo=windows)](https://github.com/NexusMemoryTrust-dev-groupe/nexus/releases)
[![Tests](https://img.shields.io/badge/tests-1085%20passing-2ea043?style=for-the-badge)](#качество-и-тестирование)
[![License](https://img.shields.io/badge/license-PROPRIETARY-d73a49?style=for-the-badge)](LICENSE)
[![MCP](https://img.shields.io/badge/MCP-143%20tools-a855f7?style=for-the-badge)](#mcp-сервер--подключение-любого-ии)

**Developed by NexusTeam**
**CEO & Founder — Danila Sergeevich Shanin**

[Скачать](#-установка) · [Бенчмарки](BENCHMARKS.md) · [Документация](#-содержание) · [MCP-сервер](#mcp-сервер--подключение-любого-ии)

</div>

---

## Результаты одним взглядом

**Измерения реального движка на 1320 реальных файлах (requests, rust-log, Material-UI).**
Ни одного мока — полная методика в [BENCHMARKS.md](BENCHMARKS.md).

| Метрика | Результат | Конкуренты |
|:---|:---:|:---|
| Поиск: нужный файл в top-5 (R@5) | **0.95** | нет семантического поиска |
| Поиск: точный файл на 1-м месте | **93.2%** | — |
| Сокращение токенов контекста | **94.2%** | ~0% (сырые файлы) |
| Классификация когнитивных слоёв | **100%** | — |
| Атак отражено (security pack) | **55 / 55** | — |
| Ложные срабатывания конфликтов | **0** | — |
| Вставка 100 000 записей (батч-транзакции) | **4 795/с** | — |
| Сборка контекста (release) | **106 ms** | — |

> **Nexus — не заметки.** Obsidian и Notion хранят текст для человека. Nexus — операционная
> система памяти для ИИ: строит граф знаний, собирает контекст под бюджет токенов, объясняет
> **почему** каждый фрагмент попал в ответ — и не пускает агента туда, куда не нужно.
> [Сравнение с Obsidian и Notion →](BENCHMARKS.md#6-тривиальный-вопрос-а-кому-это-нужно-nexus-vs-obsidian-vs-notion)

---

<div align="center">

## Поддержать проект

**Nexus разрабатывается небольшой командой без внешнего финансирования.**
Каждый донат идёт напрямую в разработку: подпись кода, инфраструктура, новые модели.

</div>

<table align="center">
<tr>
<td width="50%" align="center">

### Криптовалюта

**ByBit / TRC-20 (USDT)**

```
TP4SPiuxgw2uXYyxAhq2VBxw16UcLuaQ21
```

Сеть: **Tron (TRC-20)**
Принимаем: USDT, USDC, TRX

</td>
<td width="50%" align="center">

### Связь и поддержка

**Telegram**

### [@nexus_memory_team](https://t.me/nexus_memory_team)

Вопросы, предложения, партнёрство,
корпоративные лицензии

</td>
</tr>
</table>

<div align="center">

*Если Nexus сэкономил вам токены и время — поддержите проект.
Даже небольшой донат помогает нам двигаться дальше.*

</div>

---

## Содержание

- [Что такое Nexus](#-что-такое-nexus)
- [Почему это новая парадигма](#-почему-это-новая-парадигма)
- [Бенчмарки и сравнение](#-результаты-одним-взглядом)
- [Установка](#-установка)
- [Предупреждение Windows SmartScreen](#-предупреждение-windows-smartscreen--это-нормально)
- [Первый запуск](#-первый-запуск)
- [Возможности](#-возможности)
- [MCP-сервер](#mcp-сервер--подключение-любого-ии)
- [Экономия токенов](#-экономия-токенов)
- [Безопасность](#-безопасность)
- [Горячие клавиши](#-горячие-клавиши)
- [Архитектура](#-архитектура)
- [Качество и тестирование](#качество-и-тестирование)
- [Журнал изменений](#-журнал-изменений)
- [Частые вопросы](#-частые-вопросы)
- [Команда](#-команда)
- [Лицензия](#-лицензия)

---

## Что такое Nexus

Nexus даёт AI-кодинг-агентам **постоянную память о вашем проекте** и показывает, **почему** каждый фрагмент контекста был выбран.

Nexus — это **операционная система памяти**. Не заметки. Не база знаний. Не ещё один редактор markdown.

Проблема, которую он решает, знакома каждому, кто работает с ИИ: **модель ничего не помнит**. Каждый диалог начинается с нуля. Вы объясняете контекст заново. Вы вставляете одни и те же файлы. Вы платите за одни и те же токены снова и снова.

Стандартный подход — «загрузить в контекст побольше». Это дорого, медленно и всё равно не работает: модель тонет в шуме и теряет главное.

**Nexus делает наоборот.** Он строит граф знаний из вашей работы, ранжирует его по релевантности к текущей задаче, сжимает до бюджета токенов — и отдаёт модели плотный, точный пакет контекста вместо горы сырых файлов.

<div align="center">

| Без Nexus | С Nexus |
|:---|:---|
| 48 000 токенов на запрос | 11 800 токенов на запрос |
| 14 файлов «на всякий случай» | 9 релевантных фрагментов |
| 3 нерелевантных предположения | все источники видимы |
| 1 пропущенное архитектурное решение | решение найдено в памяти предыдущей сессии |
| Контекст умирает с закрытием чата | Память живёт между сессиями |
| Непонятно, почему ИИ ответил мимо | Видно, **почему** каждый фрагмент попал в контекст |

</div>

---

## Почему это новая парадигма

Четыре вещи, которых нет ни в одном другом продукте на рынке.

### 1. Провенанс: вы видите, **почему** ИИ получил именно этот контекст

Любая RAG-система — чёрный ящик. Модель отвечает мимо, и отладить нечего.

Nexus показывает полную трассировку каждого элемента:

**Почему попало:**
- `QueryMatch` — прямое совпадение с запросом
- `KeywordMatch` — совпадение по ключевым словам
- `GraphExpansion` — подтянуто по связи в графе (с указанием, от какой сущности)
- `MemorySearch` — найдено семантическим поиском
- `RecentActivity` — недавно изменялось
- `HighImportance` — высокий приоритет

**Почему НЕ попало:**
- `BelowRelevance` — не прошло порог релевантности
- `TokenBudget` — не хватило бюджета токенов
- `EntityCap` — превышен лимит сущностей

Плюс разбивка скора по слагаемым: сколько дал текстовый матч, сколько граф, сколько свежесть.

> Большинство решений показывают найденные источники, но не объясняют полный процесс включения и отбрасывания контекста. Nexus делает этот процесс наблюдаемым.

### 2. Измеренная экономия, а не маркетинговая

Большинство продуктов пишут «экономит до 80% токенов» и не объясняют, откуда цифра.

Nexus считает токены токенизатором **целевой модели**:
1. Считает baseline **до** сжатия
2. Считает результат **после**
3. Пишет разницу в базу вместе с полем `token_method`

Для GPT-моделей используется офлайн-токенизатор tiktoken (без сети), для embedding/local — тот же BPE-токенизатор, что в embedding-модели, для Claude и Gemini — консервативная символьная оценка.

Поле `token_method` честно говорит `exact` или `estimated`. Если точный токенизатор модели недоступен — вы это увидите, а не получите красивую ложь.

### 3. MCP как первоклассная функция

**143 инструмента** доступны любому ИИ через Model Context Protocol. Автоматическая регистрация в один клик — не нужно вручную править JSON-конфиги.

Ваш ИИ-ассистент получает не просто «доступ к заметкам», а полноценный API к графу знаний: поиск, обход связей, создание сущностей, сборка контекста, файловые операции в песочнице.

### 4. Управление актуальностью и противоречиями

Память не только хранится — она **живёт**. Nexus отслеживает жизненный цикл каждого воспоминания:

- **Подтверждение** — вы подтверждаете решение, и оно получает статус `UserConfirmed`
- **Устаревание** — устаревшее решение помечается как `Superseded` с указанием, что именно оно заменило
- **Конфликты** — противоречащие записи подсвечиваются как `Conflicted`, чтобы модель не опиралась на устаревшие факты
- **Обратная связь** — оценки `useful / irrelevant / wrong` корректируют вес памяти и число исправлений попадает в продуктовые метрики
- **Сроки** — воспоминания могут иметь `expires_at`, после которого они не участвуют в контексте

Всё это доступно через MCP (`nexus_memory_set_state`, `nexus_memory_confirm`, `nexus_memory_feedback`, `nexus_memory_supersede`), через slash-команды копилота и через экран памяти в интерфейсе. Так слово Trust в названии — не маркетинг, а реальное техническое преимущество: **система актуальности, конфликтов и подтверждения памяти**.

---

## Установка

> **Только Windows 10 / 11.** Поддерживаются архитектуры **x64** и **ARM64**.
> 32-битные системы не поддерживаются — ONNX-движок для семантического поиска не имеет 32-битной сборки.

### Способ 1 — Установщик (рекомендуется)

Самый простой путь. Скачайте и запустите.

<div align="center">

### [Скачать последнюю версию](https://github.com/NexusMemoryTrust-dev-groupe/nexus/releases/latest)

</div>

| Ваша система | Файл |
|:---|:---|
| Windows 10/11, Intel или AMD | `Nexus_1.2.0_x64-setup.exe` |
| Windows 11 на ARM (Surface, Snapdragon) | `Nexus_1.2.0_arm64-setup.exe` |
| Корпоративное развёртывание (GPO/SCCM), x64 | `Nexus_1.2.0_x64_en-US.msi` |
| Корпоративное развёртывание (GPO/SCCM), ARM64 | `Nexus_1.2.0_arm64_en-US.msi` |

> Имена файлов содержат номер версии — в новых релизах он меняется.
> Скачивайте всегда со страницы [последнего релиза](https://github.com/NexusMemoryTrust-dev-groupe/nexus/releases/latest).

Установщик:
- Ставит приложение и создаёт ярлыки
- Предлагает выбор языка (русский / английский)
- Устанавливает WebView2, если его нет
- Работает как для одного пользователя, так и для всей машины

### Способ 2 — npm

Если у вас установлен Node.js 18+:

```bash
npm install -g nexus-memory-trust
nexus
```

Пакет скачает нужный установщик под вашу архитектуру, проверит SHA-256 и запустит установку. Команда `nexus` затем открывает приложение.

### Способ 3 — PowerShell

```powershell
irm https://raw.githubusercontent.com/NexusMemoryTrust-dev-groupe/nexus/master/npm-package/scripts/install-nexus.ps1 | iex
```

Скрипт определяет архитектуру, скачивает свежий релиз, сверяет контрольную сумму и устанавливает.

### Проверка контрольной суммы

К каждому релизу прилагается `SHA256SUMS.txt`. Проверить вручную:

```powershell
Get-FileHash .\Nexus_1.2.0_x64-setup.exe -Algorithm SHA256
```

---

## Предупреждение Windows SmartScreen — это нормально

**Важно прочитать до установки. Вы увидите синее окно с предупреждением. Это ожидаемо.**

### Почему это происходит

Windows показывает предупреждение SmartScreen для программ, которые **не подписаны сертификатом Extended Validation (EV)**. Такой сертификат стоит от 300 до 600 долларов в год и требует юридической верификации организации.

Nexus — публичный релиз независимой команды. **Сертификата у нас пока нет.** Мы копим на него, в том числе на донаты.

**Это не значит, что программа опасна.** Это значит, что Microsoft ещё не видел достаточно установок, чтобы построить репутацию файла. Ровно то же самое видят пользователи любого нового инди-приложения.

### Как установить

<div align="center">

**«Windows защитила ваш компьютер»**

Нажмите **«Подробнее»** → появится кнопка **«Выполнить в любом случае»**

</div>

Пошагово:
1. Запустите скачанный `.exe`
2. Появится синее окно SmartScreen
3. Нажмите **«Подробнее»** (More info) — мелкая ссылка в окне
4. Нажмите **«Выполнить в любом случае»** (Run anyway)
5. Дальше обычная установка

### Как убедиться, что файл настоящий

Не верьте на слово — проверьте:

1. **Скачивайте только с официальных Releases** этого репозитория
2. **Сверьте SHA-256** с файлом `SHA256SUMS.txt` из того же релиза
3. **Проверьте на VirusTotal** — загрузите файл на [virustotal.com](https://www.virustotal.com)
4. **Исходный код открыт** для аудита — вы читаете его прямо сейчас

> Мы будем убирать это предупреждение по мере роста репутации файла и покупки EV-сертификата. Спасибо за терпение.

---

## Первый запуск

При первом старте открывается **мастер настройки** — 7 шагов, всё автоматически.

<div align="center">

| Шаг | Что происходит | Автоматически? |
|:---:|:---|:---:|
| 1 | Приветствие | — |
| 2 | Проверка Node.js 18+ | Проверка |
| 3 | Проверка / установка OpenCode CLI | Да, одна кнопка |
| 4 | Ввод API-ключа | Ссылка на бесплатный ключ |
| 5 | Выбор модели | Список загружается сам |
| 6 | Регистрация MCP-сервера | Да, одна кнопка |
| 7 | Готово | — |

</div>

Мастер полностью на русском и английском. Каждый шаг объясняет, что он делает и зачем.

### Где хранятся данные

```
%LOCALAPPDATA%\Nexus\
├── nexus.db                  База данных (SQLite, все ваши данные)
├── nexus.db-wal              Журнал транзакций
└── .fastembed_cache\         ONNX-модель для семантического поиска (~90 МБ)
```

**Всё локально.** Никаких облаков, никакой телеметрии, никакой отправки ваших данных куда-либо.

> Модель для семантического поиска (~90 МБ) скачивается при первом поиске, а не при установке. Первый семантический запрос будет с задержкой — это одноразово.

---

## Возможности

### Память

Записи с богатой структурой: заголовок, содержимое, автор, теги, важность, уверенность.

- **Когнитивные слои** — каждая запись автоматически классифицируется по шести слоям (Working → Episodic → Semantic → Procedural → Decision → Strategic) детерминированным классификатором; ручной выбор закрепляет слой
- **Провенанс слоя** — уверенность, причина и полная история назначений; виден в карточке и детализации записи
- **Версионирование** — полная история изменений, откат к любой версии
- **Снимки** — точки восстановления состояния
- **Граф версий** — видно, какая запись породила какую
- **Дедупликация** — Nexus не даст создать дубликат

### Граф знаний

Сущности и связи, извлечённые из вашей работы автоматически.

**Типы сущностей:** Person, Organization, Project, Document, Meeting, Decision, Task, Technology, Incident, Repository, Service, Model, Conversation, Memory (+ Custom)

**Типы связей:** Created, Modified, ParticipatedIn, DependsOn, CausedBy, RelatedTo, Owns, Uses, Mentions, DerivedFrom, BlockedBy, ReplacedBy (+ Custom)

**3D-визуализация:**
- Космическая сцена: сущности как планеты на орбитах вокруг кластеров
- **LOD-система** — три уровня геометрии (24×24, 12×12, 8×8 сегментов) по расстоянию до камеры, отсечение за порогом видимости, бюджет подписей и рёбер. Граф остаётся плавным на тысячах узлов
- Поиск с группировкой по типам, подсказки, навигация клавиатурой
- Контекстное меню: фокус, раскрытие связей, спросить копилота
- **«Показать в графе» из контекста** — собранные для запроса сущности подсвечиваются в сцене вместе с соседями (одноразовый фокус-запрос из `ContextView`)

### Сборка контекста

Ядро продукта. Вы задаёте вопрос — Nexus собирает пакет.

1. **Определение интента** — что вы вообще спрашиваете
2. **Затравка из графа** — находит точки входа
3. **Расширение по связям** — подтягивает соседей
4. **Ранжирование** — скор по релевантности, свежести, важности
5. **Внедрение памяти** — добавляет релевантные записи
6. **Сжатие** — режет до бюджета токенов
7. **Провенанс** — записывает, почему каждый элемент здесь

**Экспорт** в Markdown, JSON или plain text — одна кнопка.

### Контекстная цепочка — «Почему ИИ так сказал?» (System 5)

Каждый ответ модели записывается как **контекстная цепочка**: какие затравки памяти (seeds), какие этапы конвейера (request → seeds → expand → inject → compress → answer) и с какой уверенностью она была собрана.

- `/why <chain-id>` — текстовый разбор с ASCII-барами долей категорий (архитектура, решения, код, семантика, история, рабочая память) и самыми влиятельными затравками
- `/context-chain recent [limit]` — последние цепочки; `record` — запись вручную; `get` — полный разбор
- MCP: `nexus_why`, `nexus_context_chain_record`, `nexus_context_chain_recent`
- Чёрный ящик становится прозрачным: любой прошлый ответ можно открыть и увидеть, почему он такой

### Каноническая консолидация — дистилляция повторяющегося (System 3)

`/rehearsal-consolidate` находит кластеры похожих воспоминаний (Jaccard-сходство токенов, порог 0.40) и создаёт **канонические воспоминания**: заголовок от самого важного участника, резюме из уникальных токенов кластера, усиленные важность и уверенность.

- Исходники не удаляются — помечаются `Merged` + `superseded_by_id` и уходят в приватное
- `/canonicals [limit]` — список канонов; MCP: `nexus_rehearsal_consolidate`, `nexus_canonical_memories`
- Повторный запуск идемпотентен — кластер не пересоздаётся

### Агентные права доступа к памяти (System 4)

Политики уровня агента: какие категории (секреты, личное, архитектура, код, решения, документация) и слои (working…strategic) агент может видеть.

- `/agent-policy add <agent> [vis] [layers] : <deny-паттерны>` — создать политику
- `/agent-access <agent> <memory-id>` — проверить, увидит ли агент запись до попадания в контекст LLM
- Безопасность по умолчанию: выключенная политика = **Deny**
- MCP: `nexus_agent_policy_add`, `nexus_agent_policy_list`, `nexus_agent_access_check`

### Карта знаний — AI Universe (System 9)

`/map <entity-id> [depth]` — рендер графа вокруг сущности четырьмя кольцами: **Mission** (напрямую релевантные), **Relevant** (вторая степень), **Supporting** (третья и глубже), **Historical** (устаревшие/замещённые).

- MCP: `nexus_knowledge_map` — карта по id сущности с рендером в текст
- Плоский список соседей превращается в картину «зачем эта сущность важна»

### Экраны систем — все 10 систем в интерфейсе

Каждая флагманская система получила собственный экран (секция **Systems** в сайдбаре):

- **Rehearsal** (System 3) — план повторения, цикл, канонические воспоминания и консолидация
- **Firewall** (System 4) — пробник проверки контента, правила, карантин, политики агентов
- **Context Lab** (System 6) — A/B/C-стратегии сборки контекста с метриками и победителем
- **Passports** (System 7) — паспорта агентов: создание, активация, права доступа
- **Skills** (System 8) — список скиллов, запуск, Skill Genesis (сканирование и approve/reject)
- **Predictive** (System 9) — предсказание следующего запроса и сущностей для прогрева
- **Knowledge Map** (System 10) — карта вокруг сущности четырьмя кольцами
- Why-Chains (System 5) — секция объяснимости ответов прямо во Flight Recorder

### ИИ-копилот

Встроенный ассистент с доступом ко всей вашей памяти.

- Стриминг ответа по мере генерации
- Процесс выполнения и трассировка действий агента (этапы, вызовы инструментов, план)
- Выбор модели, включая **бесплатные**
- 143 инструмента для работы с графом, памятью и файлами

### Проекты и файлы

- Рабочие области с деревом файлов
- Редактор с подсветкой синтаксиса (JS/TS, Python, Rust, Go, Java, C/C++, JSON, YAML, Markdown, HTML, CSS, SQL и другие)
- Drag-and-drop, переименование, создание, удаление
- Автоматическая индексация файлов в граф
- WYSIWYG-редактор для документов (таблицы, чеклисты, код, изображения)

### Экономия

Отдельный экран с измеренной статистикой:

- Сэкономлено токенов: всего / сегодня / неделя / месяц
- Экономия в долларах по ценам моделей
- Разбивка по моделям
- Метод подсчёта: `exact` или `estimated` — честно

### Продуктовые метрики

Не «количество узлов», а метрики, которые реально доказывают ценность памяти:

| Метрика | Что показывает |
|:---|:---|
| Доля автоконтекста | % запросов, где контекст не добавляли вручную |
| Precision контекста | доля релевантного в собранном контексте |
| Использованные фрагменты | % фрагментов, реально использованных в ответе |
| Нерелевантные фрагменты | сколько мусора попало в контекст |
| Экономия токенов | относительно baseline до сжатия |
| Устаревшие воспоминания | сколько записей потеряло актуальность |
| Исправления памяти | сколько раз пользователь поправил/подтвердил |
| Latency сборки | среднее время сборки контекста |
| Повторное использование | воспоминания, использованные между сессиями |

Экран экономии показывает эти метрики в реальном времени через `get_product_metrics`.

### Двуязычный интерфейс

Полностью русский и английский. Переключение на лету, без перезапуска.

---

## MCP-сервер — подключение любого ИИ

Nexus работает как **MCP-сервер** (Model Context Protocol) и отдаёт **143 инструмента** любому совместимому ИИ.

### Автоматическая регистрация

В мастере настройки или в настройках — кнопка **«Зарегистрировать MCP»**. Nexus сам правит конфиг OpenCode, делает резервную копию, проверяет результат.

### Ручное подключение

<details>
<summary><b>OpenCode</b></summary>

Файл: `%USERPROFILE%\.config\opencode\opencode.json`

```json
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "nexus": {
      "type": "local",
      "command": [
        "C:\\Program Files\\Nexus\\Nexus.exe",
        "--mcp"
      ],
      "enabled": true
    }
  }
}
```

Проверка:
```bash
opencode mcp list
```
Должно показать `✓ nexus connected`.

</details>

<details>
<summary><b>Claude Desktop</b></summary>

Файл: `%APPDATA%\Claude\claude_desktop_config.json`

```json
{
  "mcpServers": {
    "nexus": {
      "command": "C:\\Program Files\\Nexus\\Nexus.exe",
      "args": ["--mcp"]
    }
  }
}
```

Перезапустите Claude Desktop.

</details>

<details>
<summary><b>Cursor</b></summary>

Файл: `%USERPROFILE%\.cursor\mcp.json`

```json
{
  "mcpServers": {
    "nexus": {
      "command": "C:\\Program Files\\Nexus\\Nexus.exe",
      "args": ["--mcp"]
    }
  }
}
```

</details>

<details>
<summary><b>Continue (VS Code / JetBrains)</b></summary>

Файл: `%USERPROFILE%\.continue\config.json`

```json
{
  "experimental": {
    "modelContextProtocolServers": [
      {
        "transport": {
          "type": "stdio",
          "command": "C:\\Program Files\\Nexus\\Nexus.exe",
          "args": ["--mcp"]
        }
      }
    ]
  }
}
```

</details>

<details>
<summary><b>Node.js / собственный агент</b></summary>

```javascript
const { spawn } = require('child_process');

const nexus = spawn('C:\\Program Files\\Nexus\\Nexus.exe', ['--mcp']);

// Список инструментов
nexus.stdin.write(JSON.stringify({
  jsonrpc: '2.0', id: 1, method: 'tools/list'
}) + '\n');

// Сборка контекста
nexus.stdin.write(JSON.stringify({
  jsonrpc: '2.0', id: 2, method: 'tools/call',
  params: {
    name: 'nexus_build_context',
    arguments: { query: 'архитектура аутентификации', max_tokens: 4000 }
  }
}) + '\n');

nexus.stdout.on('data', (d) => console.log(d.toString()));
```

</details>

### Все 143 инструмента

<details>
<summary><b>Память (10)</b></summary>

`nexus_list_memories` · `nexus_get_memory` · `nexus_create_memory` · `nexus_update_memory` · `nexus_delete_memory` · `nexus_search_memories` · `nexus_search_semantic` · `nexus_get_recent_memories` · `nexus_get_important_memories` · `nexus_store_fingerprint`

</details>

<details>
<summary><b>Когнитивные слои памяти (5)</b></summary>

`nexus_layers_list` · `nexus_layer_stats` · `nexus_layer_set` · `nexus_layer_reclassify` · `nexus_layer_history`

Система 1: каждая запись классифицируется по шести когнитивным слоям — Working → Episodic → Semantic → Procedural → Decision → Strategic. Детерминированный сигнатурный классификатор (RU+EN) присваивает слой при создании и записывает провенанс: уверенность, причину и полную историю назначений. Ручной выбор закрепляет слой и не перезаписывается классификатором.

</details>

<details>
<summary><b>Жизненный цикл памяти (5)</b></summary>

`nexus_memory_set_state` · `nexus_memory_confirm` · `nexus_memory_feedback` · `nexus_memory_supersede` · `nexus_lifecycle_overview`

Управление актуальностью воспоминаний: подтверждение, устаревание (`supersede`), конфликты, обратная связь по качеству — и обзор всего жизненного цикла.

</details>

<details>
<summary><b>Сущности — дедупликация (2)</b></summary>

`nexus_find_duplicates` · `nexus_merge_entities`

Поиск и слияние дублирующихся сущностей графа (транзитивные группы через union-find, лучший ID — по самому длинному имени).

</details>

<details>
<summary><b>Граф знаний (10)</b></summary>

`nexus_graph_stats` · `nexus_get_entity` · `nexus_create_entity` · `nexus_update_entity` · `nexus_delete_entity` · `nexus_link_entities` · `nexus_unlink_entities` · `nexus_list_graph_entities` · `nexus_entity_metadata` · `nexus_link_project_entity`

</details>

<details>
<summary><b>Контекст и поиск (5)</b></summary>

`nexus_build_context` · `nexus_build_context_for_entity` · `nexus_search_context` · `nexus_analyze_text` · `nexus_parse_markdown`

</details>

<details>
<summary><b>Связи память ↔ сущность (4)</b></summary>

`nexus_link_memory_entity` · `nexus_unlink_memory_entity` · `nexus_get_memory_links` · `nexus_get_entity_memory_links`

</details>

<details>
<summary><b>Проекты (3)</b></summary>

`nexus_projects` · `nexus_project_entities` · `nexus_project_memories`

</details>

<details>
<summary><b>Рабочая область (8)</b></summary>

`nexus_add_to_workspace` · `nexus_get_workspace` · `nexus_sync_workspace` · `nexus_workspace_check_stale` · `nexus_workspace_rename` · `nexus_workspace_move` · `nexus_workspace_delete` · `nexus_workspace_remove`

</details>

<details>
<summary><b>Файлы — все в песочнице (13)</b></summary>

`nexus_index_file` · `nexus_index_folder` · `nexus_scan_folder` · `nexus_read_file_content` · `nexus_read_file` · `nexus_create_file` · `nexus_write_file` · `nexus_create_folder` · `nexus_delete_file` · `nexus_delete_folder` · `nexus_rename_file` · `nexus_move_file` · `nexus_create_workspace_file`

</details>

<details>
<summary><b>Экономия токенов и продуктовые метрики (5)</b></summary>

`nexus_savings_stats` · `nexus_savings_report` · `nexus_savings_per_model` · `nexus_savings_record` · `nexus_product_metrics`

</details>

<details>
<summary><b>Система и диагностика (9)</b></summary>

`nexus_copilot_command` · `nexus_stats` · `nexus_db_stats` · `nexus_health` · `nexus_settings` · `nexus_config_get` · `nexus_config_set` · `nexus_timeline` · `nexus_ai_models`

</details>

<details>
<summary><b>Документация (3)</b></summary>

`nexus_docs_import` · `nexus_docs_list` · `nexus_docs_search`

</details>

<details>
<summary><b>Агенты (2)</b></summary>

`nexus_agents_read` · `nexus_agents_generate`

</details>

<details>
<summary><b>Скиллы (2)</b></summary>

`nexus_skills_list` · `nexus_skills_run`

</details>

<details>
<summary><b>Код (5)</b></summary>

`nexus_code_import` · `nexus_code_list` · `nexus_code_search` · `nexus_code_deps` · `nexus_code_dependents`

</details>

<details>
<summary><b>Радар (1)</b></summary>

`nexus_radar_snapshot`

</details>

<details>
<summary><b>Команда (5)</b></summary>

`nexus_team_add_member` · `nexus_team_list_members` · `nexus_team_update_member` · `nexus_team_remove_member` · `nexus_team_overview`

</details>

<details>
<summary><b>Аудит (3)</b></summary>

`nexus_audit_trail` · `nexus_audit_add_event` · `nexus_audit_alternative`

</details>

### Пример: ИИ работает с вашей памятью

```
Вы: Что мы решили по поводу аутентификации?

ИИ вызывает nexus_build_context { query: "решение по аутентификации" }

Nexus возвращает:
  · Запись «Выбор JWT vs сессии» (14.03, важность 0.9)
  · Сущность «AuthService» — связана с «JWT», «Redis»
  · Запись «Обсуждение refresh-токенов» (16.03)
  · Провенанс: QueryMatch + GraphExpansion от AuthService
  · Токенов: 1 240 из 4 000 (baseline был 8 900)

ИИ: Вы выбрали JWT с refresh-токенами в Redis.
    Решение от 14 марта, основная причина — горизонтальное
    масштабирование без общего состояния сессий.
```

---

## Экономия токенов

### Как считается

Не «примерно», а точно:

1. **Baseline** — сколько токенов заняли бы сырые данные. Считается **до** сжатия
2. **Результат** — сколько заняло сжатое
3. **Разница** — записывается в базу с указанием метода

### Токенизатор

Счёт ведётся токенизатором **выбранной модели**, а не эвристикой `длина / 4`:

| Модель | Токенизатор | Метод |
|:---|:---|:---|
| GPT (o-series, gpt-4o и др.) | tiktoken-rs, офлайн, без сети | `exact` |
| Embedding / Local (ONNX) | BPE из embedding-модели | `exact` |
| Claude / Gemini | консервативная символьная оценка | `estimated` |

Почему это важно:

| Текст | Эвристика `len/4` | Реальность | Ошибка |
|:---|:---|:---|:---|
| Русский текст | Занижает в ~2 раза | 1 токен ≈ 2–3 символа | **до 200%** |
| Код с пунктуацией | Занижает | Много токенов на символы | до 150% |
| Английский ASCII | Примерно верно | ~4 символа на токен | ~10% |

Символьные эвристики особенно неточны для кириллицы и смешанного кода, поэтому Nexus использует модель-специфичный токенизатор там, где он доступен: **иначе цифра экономии в интерфейсе была бы вымыслом.**

### Поле `token_method`

Интерфейс всегда показывает, как получена цифра:
- `exact` — реальный BPE-подсчёт токенизатором модели
- `estimated` — точный токенизатор модели недоступен, использована консервативная оценка

Мы не выдаём оценку за точность.

---

## Безопасность

### Песочница файловых операций

Все 13 файловых инструментов MCP проходят через **whitelist-проверку путей**. ИИ-модель не может читать или писать вне разрешённых папок.

**Разрешено:**
- Папки, добавленные вами в рабочую область проекта
- Дополнительные корни из настроек (`sandbox.extra_roots`)
- Папка данных Nexus

**Блокируется:**
- Абсолютные пути вне разрешённых корней
- Обход через `..`
- Относительные пути
- Зарезервированные имена Windows (`CON`, `PRN`, `AUX`, `NUL`, `COM1`–`COM9`, `LPT1`–`LPT9`)
- Символические ссылки и NTFS-джанкшены, ведущие наружу

Проверка идёт **после** канонизации пути, поэтому текстовые трюки не помогают.

**Проверено живьём:**

```
nexus_write_file C:\Windows\System32\pwn.txt
  → Отказано: путь вне рабочей области Nexus

nexus_index_file C:\Windows\win.ini
  → Отказано: путь вне рабочей области Nexus

nexus_read_file ..\..\..\Windows\System32\config\SAM
  → Отказано: путь не абсолютный
```

### Приватность

- **Всё локально.** SQLite на вашем диске
- **Никакой телеметрии.** Ни одного исходящего запроса, кроме вызовов ИИ, которые вы сами инициируете
- **API-ключи** хранятся в credential-store OpenCode, не в нашей базе
- **Никакой аналитики**, трекеров, «анонимной статистики использования»

### Безопасность базы данных

- `PRAGMA foreign_keys = ON` — целостность связей
- `busy_timeout = 5000` — конкурентные записи ждут, а не падают
- Режим WAL — устойчивость к сбоям
- 18 версионированных миграций, вкомпилированных в бинарник

---

## Горячие клавиши

**Работают на любой раскладке.** Мы используем `e.code` (физическая позиция клавиши), а не `e.key` — поэтому Ctrl+K работает и когда у вас включён русский, и когда английский.

<div align="center">

| Клавиши | Действие |
|:---|:---|
| `Ctrl` + `K` | Командная строка |
| `Ctrl` + `1` | Память |
| `Ctrl` + `2` | Граф |
| `Ctrl` + `3` | Хронология |
| `Ctrl` + `4` | Проекты |
| `Ctrl` + `5` | Экономия |
| `Ctrl` + `6` | Контекст |
| `Ctrl` + `,` | Настройки |
| `Ctrl` + `N` | Новая запись |
| `Ctrl` + `R` / `F5` | Обновить |
| `Ctrl` + `S` | Сохранить файл |
| `Ctrl` + `Z` / `Y` | Отменить / повторить |
| `Ctrl` + `B` / `I` | Жирный / курсив |
| `Esc` | Закрыть панель |

</div>

---

## Архитектура

```
┌──────────────────────────────────────────────────────────┐
│  React 19 + TypeScript + Three.js                        │
│  Граф · Память · Контекст · Проекты · Экономия            │
└────────────────────────┬─────────────────────────────────┘
                         │ Tauri IPC
┌────────────────────────┴─────────────────────────────────┐
│  Rust — ядро                                             │
│                                                          │
│  Контекст:   intent → seed → expand → rank → compress    │
│  Провенанс:  трассировка причин включения и отбрасывания  │
│  Токенизатор: BPE из embedding-модели                    │
│  Песочница:  whitelist путей для всех файловых операций   │
│  Индексатор: фоновые эмбеддинги                          │
│  MCP-сервер: 143 инструмента через stdio                   │
└────────────────────────┬─────────────────────────────────┘
                         │
┌────────────────────────┴─────────────────────────────────┐
│  SQLite (WAL) + ONNX (all-MiniLM-L6-v2, 384-мерные)      │
│  18 миграций, вкомпилированных в бинарник                 │
└──────────────────────────────────────────────────────────┘
```

**Стек:** Rust · Tauri 2 · React 19 · TypeScript · Three.js · SQLite · ONNX Runtime · fastembed · tokenizers

**Принципы:**
- Ядро не знает про Tauri — слои разделены
- Репозитории за трейтами
- Миграции вкомпилированы, не читаются с диска
- Никаких `unwrap()` на пользовательском вводе
- Никаких подавлений типов (`as any`, `@ts-ignore`)

---

## Качество и тестирование

<div align="center">

| Метрика | Значение |
|:---|:---|
| Rust unit-тесты (lib) | **978** |
| Rust интеграционные (E2E, proptest, fuzz, security) | **56** |
| Frontend-тесты (vitest, LOD + слои) | **41** |
| E2E-тесты (Playwright) | **10** |
| **Итого** | **1085** |
| Ошибок TypeScript | **0** |
| Предупреждений сборки | **0** |

</div>

Тесты покрывают логику, а не геттеры: UTF-8-безопасное усечение, границы LOD-уровней, whitelist песочницы, идемпотентность MCP-регистрации, точность токенизатора, миграции схемы, трассировку провенанса, классификацию когнитивных слоёв и закрепление их пользователем. Качество держится жёсткими гейтами: `tsc` без ошибок, `cargo test`, `cargo clippy -- -D warnings`, `cargo fmt --check` — все в CI.

---

## Журнал изменений

### v1.2.0 — полноценные UI-экраны всех систем

Все 10 флагманских систем получили собственные экраны интерфейса; MCP-сервер расширен до 143 инструментов, бэкенд покрыт 792 unit-тестами и 39 тестами диспетчера MCP.

| # | Область | Что сделано | Технические детали |
|:---:|:---|:---|:---|
| 1 | Повторение (System 3) | Экран `RehearsalView` | План повторения со счётчиками (due now / never rehearsed / scheduled), запуск цикла, список канонических воспоминаний и консолидация; команды `get_rehearsal_plan`, `run_rehearsal_cycle`, `rehearse_memory`, `list_canonical_memories`, `run_canonical_consolidation` |
| 2 | Файрвол (System 4) | Экран `FirewallView` | Пробник проверки контента (toxicity/spam/injection/PII), правила блокировки/карантина, очередь карантина с approve/reject, политики агентов и deny-паттерны |
| 3 | Контекстная цепочка (System 5) | Секция Why-Chains во `FlightView` | Карточки контекстных цепочек с разбором «почему ИИ так сказал» (`chain.why` + `chain.pipeline`), счётчик в hero; `context_chain_recent` |
| 4 | Контекст-Лаб (System 6) | Экран `ContextLabView` | Прогон эксперимента: стратегии compact/balanced/rich, метрики (tokens, maturity, accuracy, efficiency), победитель и история |
| 5 | Паспорт агента (System 7) | Экран `PassportView` | Карточки паспортов, форма создания/обновления, активация активного агента, удаление |
| 6 | Скиллы (System 8) | Экран `SkillsView` | Список скиллов с запуском, Skill Genesis: сканирование журнала полёта, approve/reject кандидатов |
| 7 | Предиктор (System 9) | Экран `PredictiveView` | Предсказание следующего запроса с вероятностями, сущности для прогрева, размер истории |
| 8 | Карта знаний (System 10) | Экран `KnowledgeMapView` | Пайкер сущностей из графа, карта четырьмя кольцами (Mission/Relevant/Supporting/Historical) с рендером |
| 9 | Интеграция UI | Навигация, локали, типы | Секция «Systems» в сайдбаре (7 иконок), lazy-регистрация в `App.tsx`, ключи RU/EN в `localeStore.ts`, TS-типы DTO для всех команд, `tsc` и `vite build` чисто |
| 10 | Баги MCP | 2 реальных бага исправлены | Паника `memory_score::title_similarity` на кириллице (char-based срез вместо byte), `agent_access_check` теперь возвращает Deny вместо ошибки (безопасность по умолчанию) |
| 11 | MCP-API | 100 → 143 инструмента | 16 новых smoke-тестов диспетчера: rehearsal, firewall, context_lab, passport, skill_genesis, predictive, knowledge_map, context_chain, savings |
| 12 | Тесты | Полный прогон | `cargo test --lib` 792 passed / 0 failed, `mcp_server` 39 passed / 0 failed, `cargo check --lib` без предупреждений |

### v1.1.0 — когнитивные слои памяти

Система 1: детерминированная классификация записей по шести когнитивным слоям с полным провенансом — от «рабочей» заметки до стратегического знания.

| # | Область | Что сделано | Технические детали |
|:---:|:---|:---|:---|
| 1 | Таксономия слоёв | 6 когнитивных слоёв памяти | Миграция V18: колонки провенанса `layer_confidence`, `layer_reason`, `layer_updated_at`, `layer_history_json` + бэкфилл (Raw→Episodic, Knowledge→Semantic, Wisdom→Strategic); `LayerAssignment`, `LayerHistoryEntry`, `MemoryLayer` (6 значений) |
| 2 | Классификатор | Детерминированный сигнатурный скоринг (RU+EN) | `core/memory/layer/`: `signals.rs`, `classifier.rs`, `LAYER_CATALOG`; автоклассификация при create/update; `set_layer`/`promote`/`reclassify`/`history`/`stats` в `memory_service.rs`; ручной выбор закрепляет слой |
| 3 | MCP-API | 74 → 100 инструментов | `nexus_layers_list`, `nexus_layer_stats`, `nexus_layer_set`, `nexus_layer_reclassify`, `nexus_layer_history`; Tauri-команды в `commands/memory.rs` |
| 4 | ИИ-копилот | 5 новых slash-команд | `/layer-set`, `/layer-reclassify`, `/layer-history`, `/layer-stats`, `/layers-list` |
| 5 | Память (UI) | Слои и провенанс в интерфейсе | `layers.ts`: 6 слоёв (Working→Strategic) с визуалами и fallback; `MemoryCard`: глиф слоя + тег; `MemoryDetail`: блок провенанса — чип слоя, уверенность, причина, история назначений, кнопка Reclassify; legend и ladder на 6 слоёв |
| 6 | Локализация | Слои и провенанс в RU/EN | `pagesLocale.ts`: semantics слоёв, подсказки лестницы, ключи истории назначений (`layer.by.user/classifier/migration/unknown`) |
| 7 | Тесты | Классификатор, фронтенд, сквозной сценарий | 9 unit-тестов сервиса (провенанс, пины, история), 9 фронтенд-тестов слоёв (`layers.test.ts`), 3 интеграционных теста через реальную SQLite; исправлен баг: `reclassify` больше не перезаписывает закреплённый пользователем слой |
| 8 | Продуктовые метрики | Счётчики по слоям | `get_layer_stats()`: число записей и средняя уверенность по каждому слою; экран метрик дополнен |

### v1.0.2 — доверие к памяти

| # | Область | Что сделано | Технические детали |
|:---:|:---|:---|:---|
| 1 | Жизненный цикл памяти | Система актуальности, конфликтов и подтверждения | Миграция V12 (13 колонок): `memory_state`, `supersedes_id`, `superseded_by_id`, `confirmed_at/by`, `expires_at`, `feedback`; конфликт-детектор; `commands/lifecycle.rs` — 6 команд: `memory_set_state`, `memory_confirm`, `memory_feedback`, `memory_supersede`, `get_lifecycle_overview`, `get_memory` |
| 2 | Дедупликация сущностей | Поиск и слияние дубликатов | `resolution.rs`: транзитивные группы через union-find (6 тестов); `find_duplicate_entities`, `merge_entities` |
| 3 | Токенизатор | Честный модель-специфичный подсчёт токенов | `tokenizer.rs`: Target::Claude/Gpt/Gemini/Local/Embedding; tiktoken-rs 0.12 офлайн для GPT; HF `tokenizer.json` для Local/Embedding; `estimated` для Claude/Gemini; `select_model` вызывает `set_active_model`; 14 тестов токенизатора |
| 4 | Продуктовые метрики | Метрики, доказывающие ценность | Миграция V13: latency, precision, used/irrelevant fragments, manual_context, memory_ids; `get_product_metrics()`; замеры latency в `context.rs` и `copilot.rs` |
| 5 | MCP-API | 66 → 74 инструмента | `nexus_memory_set_state/confirm/feedback/supersede`, `nexus_lifecycle_overview`, `nexus_find_duplicates`, `nexus_merge_entities`, `nexus_product_metrics`; схемы `nexus_savings_record` дополнены |
| 6 | ИИ-копилот | 8 новых slash-команд | `memory-set-state`, `memory-confirm`, `memory-feedback`, `memory-supersede`, `lifecycle-overview`, `find-duplicates`, `merge-entities`, `product-metrics` |
| 7 | Память (UI) | Секция «Доверие» в детализации | `MemoryDetail`: бейдж состояния, кнопки Confirm / Useful / Irrelevant / Wrong, метаданные подтверждения и замены; `memoryStore`: экшены lifecycle |
| 8 | Контекст (UI) | Арифметика скора и переход в граф | `ContextView`: раскрываемая разбивка `scoreParts` по компонентам; кнопка «Показать в графе» через одноразовый `focusRequest` |
| 9 | Экономия (UI) | Экран продуктовых метрик | `SavingsView`: `ProductMetricsSection` — автоконтекст, precision, использованные фрагменты, latency, устаревшие, исправления, повторное использование |

### v1.0.1 — предыдущий релиз

| # | Область | Что сделано | Технические детали |
|:---:|:---|:---|:---|
| 1 | 3D-граф | Исправлена физика космической сцены | `CosmicGraphView`: ленивая инициализация вектора скорости `vj` в цикле физики устраняет `TypeError` на каждом кадре — граф снова анимируется |
| 2 | Tauri IPC / ACL | Разблокированы события ИИ-копилота | Новый манифест `capabilities/default.json` включает `core:event:default`; стриминговые события `ai-thinking-chunk`, `ai-text-chunk`, `ai-stream-finish` проходят через ACL (`event\|listen not allowed by ACL` устранено) |
| 3 | ИИ-копилот | Исправлена отправка повторного Enter | `FloatingCopilot`: палитра команд после выбора команды остаётся закрытой (`setShowCommands(input.startsWith('/') && input.length > 0 && !input.endsWith(' '))`) — повторный Enter отправляет команду вместо повторного открытия палитры |
| 4 | MCP-API | Расширен с 48 до 66 инструментов | Добавлены `nexus_projects`, `nexus_project_entities`, `nexus_project_memories`, `nexus_link_project_entity`, `nexus_workspace_rename/move/delete/remove/check_stale`, `nexus_rename_file`, `nexus_delete_folder`, `nexus_scan_folder`, `nexus_entity_metadata`, `nexus_savings_record`, `nexus_ai_models`, `nexus_db_stats`, `nexus_config_get`, `nexus_config_set` |
| 5 | ИИ-копилот | Новые slash-команды | `create-file`, `write-file`, `create-folder`, `read-file`, `index-file`, `index-folder`, `workspace-file`, `entity-meta`, `config-get`, `config-set`, `file-rename`, `file-delete-folder` |
| 6 | Контекст (UI) | Полностью переписан конвейер сборки контекста | `ContextView`: модульная компоновка пайплайна, многослойные strata-панели, компонент `Instruments`, удалён устаревший `WhyPanel`; добавлены `layers.ts`, `useMotion.ts`, `format.ts` |
| 7 | Локализация | Расширены словари RU/EN | `pagesLocale.ts`: полные копии всех новых экранов; переключение языка на лету |
| 8 | Память (UI) | Переработаны карточки и детализация записей | `MemoryCard`, `MemoryDetail`, `MemoryExplorer`: новая верстка, отображение метаданных, связей и провенанса |
| 9 | Хронология (UI) | Тепловая карта активности | `TimelineView`: heatmap по дням, плотность записей, быстрая навигация по периодам |
| 10 | Проекты (UI) | Обновлён экран проекта | `ProjectDetail`: обновлённая структура, синхронизация с новыми MCP-инструментами проектов |
| 11 | E2E-тесты | Добавлен визуальный спек | `e2e/strata-visual.spec.ts`: проверка многослойного UI, 10 E2E-тестов Playwright суммарно |
| 12 | CI/CD | Пайплайн приведён в соответствие с реальной структурой репозитория | Workflow-файлы указывают на фактические пути (`src-tauri`), ручной релиз публикуется в существующий тег, actions обновлены до v5 |

### v1.0.0 — первый публичный релиз

| # | Область | Что сделано | Технические детали |
|:---:|:---|:---|:---|
| 1 | Лицензирование | MIT заменён на проприетарную лицензию | Полный запрет копирования/переиспользования, статья 146 УК РФ и пр. — см. [LICENSE](LICENSE) |
| 2 | MCP-документация | Схемы инструментов синхронизированы с кодом | Описания `tools/list` приведены в соответствие с фактическими схемами параметров |
| 3 | Миграции БД | Сделаны идемпотентными | `ALTER TABLE ADD COLUMN` выполняется с проверкой существования колонки (`column_exists`), повторный запуск безопасен |
| 4 | Установка | npm-пакет и кросс-платформенные установщики | `npm-package` с автоматическим скачиванием установщика под архитектуру и проверкой SHA-256; NSIS + MSI для x64 и ARM64 |
| 5 | MCP-клиент | Node.js helper-библиотека | Официальная библиотека для подключения Nexus к собственным агентам на Node.js |
| 6 | Rust-ядро | Проверка модели до запроса к БД | Модель проверяется до обращения к базе; повышен порог кириллического fallback-токенизатора для честного подсчёта |
| 7 | Rust-ядро | Интент-чувствительный поиск сущностей | Поиск учитывает семантический интент; добавлено распознавание JS-стрелочных функций в индексаторе |
| 8 | Rust-ядро | Удалён мёртвый код | Выпилен неиспользуемый operator-mode модуль и `ai_chat`; `clippy -D warnings` чист |
| 9 | Frontend | Приведены в порядок шорткаты и навигация | Согласованы горячие клавиши, экран экономии в сайдбаре, экран Context в навигации |
| 10 | CI | Гейты качества запускаемы | Lint и тесты фронтенда проходят в CI; `react-hooks` предупреждения устранены |

---

## Частые вопросы

<details>
<summary><b>Нужен ли интернет?</b></summary>

Для работы с памятью и графом — нет, всё локально. Интернет нужен только для вызовов ИИ-модели и однократной загрузки embedding-модели (~90 МБ) при первом семантическом поиске.

</details>

<details>
<summary><b>Мои данные уходят на сервер?</b></summary>

Нет. Всё в SQLite на вашем диске. Единственный исходящий трафик — запросы к ИИ-модели, которые вы сами инициируете. Никакой телеметрии.

</details>

<details>
<summary><b>Это бесплатно?</b></summary>

Приложение — да. Для ИИ нужен ключ OpenCode, и там есть **бесплатные модели** (например `deepseek-v4-flash-free`). Ключ получается бесплатно на opencode.ai/auth.

</details>

<details>
<summary><b>Почему нет версии для macOS / Linux?</b></summary>

Windows — приоритет первого релиза. Кодовая база на Rust и Tauri кросс-платформенная, так что порт возможен. Сроков пока не называем.

</details>

<details>
<summary><b>Почему нет 32-битной версии?</b></summary>

ONNX Runtime, который нужен для семантического поиска, не поставляет 32-битных сборок. Обойти это без отказа от семантики нельзя.

</details>

<details>
<summary><b>Почему Windows предупреждает при установке?</b></summary>

Приложение не подписано EV-сертификатом — он стоит 300–600 долларов в год. Подробно об этом — в разделе [про SmartScreen](#-предупреждение-windows-smartscreen--это-нормально). Проверьте SHA-256 и VirusTotal, если беспокоитесь.

</details>

<details>
<summary><b>Первый семантический поиск долгий. Это нормально?</b></summary>

Да. Скачивается ONNX-модель (~90 МБ) в `%LOCALAPPDATA%\Nexus\.fastembed_cache`. Один раз. Дальше поиск быстрый.

</details>

<details>
<summary><b>Как удалить программу?</b></summary>

Параметры → Приложения → Nexus → Удалить. База данных остаётся в `%LOCALAPPDATA%\Nexus\` — удалите папку вручную, если нужно стереть всё.

</details>

<details>
<summary><b>Можно использовать в коммерческом проекте?</b></summary>

Личное и внутреннее использование — да. Перепродажа, включение в свой продукт, предоставление как сервис — **нет** без письменного соглашения. Читайте [LICENSE](LICENSE). По корпоративным лицензиям пишите в [Telegram](https://t.me/nexus_memory_team).

</details>

---

## Команда

<div align="center">

### **Developed by NexusTeam**

### CEO & Founder — **Danila Sergeevich Shanin**

*Шанин Данила Сергеевич*

---

**Связь:** [@nexus_memory_team](https://t.me/nexus_memory_team)

**Поддержать:** `TP4SPiuxgw2uXYyxAhq2VBxw16UcLuaQ21` (TRC-20)

</div>

---

## Лицензия

<div align="center">

### ПРОПРИЕТАРНОЕ ПРОГРАММНОЕ ОБЕСПЕЧЕНИЕ

**Copyright © 2026 NexusTeam / Шанин Данила Сергеевич. Все права защищены.**

</div>

Это **не** программа с открытым исходным кодом. Код опубликован для прозрачности и аудита безопасности — **не** для копирования, переиспользования или создания производных продуктов.

### Разрешено

- Устанавливать и использовать для личных и внутренних задач
- Читать исходный код для аудита и обучения
- Создавать резервные копии своих данных

### Запрещено

- Копировать, распространять, публиковать код или бинарники
- Перепродавать, сдавать в аренду, предоставлять как сервис (SaaS)
- Создавать производные продукты, форки, «свои версии»
- Убирать или изменять указания на авторство
- Обходить механизмы лицензирования
- Использовать код в своих проектах — коммерческих или нет

### Ответственность — реальные статьи закона

Нарушение прав на это ПО влечёт ответственность по действующему законодательству.

**Российская Федерация:**

| Норма | Содержание | Санкция |
|:---|:---|:---|
| **УК РФ ст. 146** | Нарушение авторских прав в крупном размере | до **6 лет** лишения свободы |
| **УК РФ ст. 272** | Неправомерный доступ к компьютерной информации | до **7 лет** лишения свободы |
| **УК РФ ст. 273** | Создание и распространение вредоносных программ | до **7 лет** лишения свободы |
| **ГК РФ ст. 1301** | Компенсация за нарушение исключительного права | от **10 тыс. до 5 млн ₽** либо двукратная стоимость |
| **КоАП РФ ст. 7.12** | Административная ответственность | штраф + конфискация |

Программы для ЭВМ охраняются как объекты авторского права — **ГК РФ ст. 1259, 1261**.

**США:** 17 U.S.C. § 501, § 506 · 18 U.S.C. § 2319 (до **10 лет**) · DMCA 17 U.S.C. § 1201 · статутные убытки до **$150 000** за каждое умышленное нарушение (17 U.S.C. § 504)

**Международно:** Бернская конвенция · Договор ВОИС по авторскому праву 1996 · Соглашение ТРИПС ст. 61 · Директива ЕС 2009/24/EC

Полный текст — в файле [LICENSE](LICENSE).

> **Мы фиксируем нарушения и передаём материалы юристам.** Публикация исходного кода — знак доверия к сообществу, а не отказ от прав.

---

<div align="center">

## Nexus Memory Trust

**Новая эра работы с памятью ИИ**

Ваш ассистент больше не начинает с нуля.

---

[Скачать](https://github.com/NexusMemoryTrust-dev-groupe/nexus/releases/latest) · [Telegram](https://t.me/nexus_memory_team) · [Лицензия](LICENSE)

**Developed by NexusTeam**
**CEO & Founder — Danila Sergeevich Shanin**

*Copyright © 2026. Все права защищены.*

</div>
